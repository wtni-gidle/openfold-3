output_writer_settings:
  # The EnsembleFold predict wrapper requires CIF for inference.
  structure_format: cif
  # Detailed confidence defaults to JSON; true selects compressed NPZ.
  compress_full_confidence: false
  # Save features (default: False) 
  write_features: True
